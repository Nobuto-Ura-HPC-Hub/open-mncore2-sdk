# test-emu-lib suite: emu:lib backend で examples を走らせる
# 必要ツール (clang / ninja / assemble3) が PATH に無ければ suite ごと UNSUPPORTED

import shutil

for tool in ['clang', 'ninja', 'assemble3']:
    if not shutil.which(tool):
        config.unsupported = True
        break
