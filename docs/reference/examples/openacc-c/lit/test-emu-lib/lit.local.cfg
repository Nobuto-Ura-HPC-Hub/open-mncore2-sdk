# test-emu-lib suite: emu:lib backend で examples を走らせる
# 必要ツール (clang / make / assemble3 / vsmlink) が PATH に無ければ suite ごと UNSUPPORTED

import shutil

for tool in ['clang', 'make', 'assemble3', 'vsmlink']:
    if not shutil.which(tool):
        config.unsupported = True
        break
