# libmnc2 の使用例

実際の使用例を通じて、libmnc2 の使い方を学びます。

## 例 1: シンプルなプログラム

MN-Core2 デバイスの基本的な情報を取得する例：

```c
#include <mnc2.h>
#include <stdio.h>

int main() {
    // 初期化処理
    // （詳細なコード例は記載予定）
    
    // デバイス情報の取得
    // （詳細なコード例は記載予定）
    
    printf("MN-Core2 デバイス情報を表示\n");
    
    return 0;
}
```

### ビルド方法

```bash
gcc -o example1 example1.c -L${MN_CORE2_SDK}/lib -lmnc2 -I${MN_CORE2_SDK}/include
./example1
```

## 例 2: メモリ転送

ホストとデバイス間のメモリ転送の例：

（詳細なコード例は記載予定）

## 例 3: カーネル実行

カーネルプログラムをデバイスで実行する例：

（詳細なコード例は記載予定）

## 例 4: パフォーマンス測定

プログラムのパフォーマンスを測定する例：

（詳細なコード例は記載予定）

## トラブルシューティング

### よくある問題

#### ライブラリが見つからない

```
error: undefined reference to `mnc2_...'
```

**原因**: libmnc2 がリンクされていない
**解決策**: コンパイルコマンドに `-lmnc2` オプションが含まれていることを確認

#### デバイスが認識されない

（その他のトラブルシューティング情報は記載予定）

## 参考資料

- [基本的な使い方](./basic-usage.md)
- [MN-Core2 とは](../01-mncore2-overview.md)
